language: python
python:
  - "3.6.2"
  
# command to install dependencies
install:
  - "pip install -r requirements.txt"
script:
  - "pytest"
after_success:
  - coveralls
notifications:
  email: false